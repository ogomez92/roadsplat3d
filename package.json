{
  "name": "roadsplat",
  "version": "4.0.3",
  "description": "Audio game",
  "main": "./index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "parcel watch ./js/main.js -d . --target=electron --no-hmr",
    "build": "parcel build ./js/main.js -d . --target=electron",
    "start": "electron .",
    "run": "npm run build && electron .",
    "git": "git add . && git commit -a -m 'auto commit' && npm version patch && git push",
    "v": "git add . && git commit -a -m 'auto commit' && npm version major && git push"
  },
  "author": "Oriol Gomez",
  "license": "GPL",
  "dependencies": {
    "@codyjdalton/game-time": "0.0.0-2",
    "babel-polyfill": "^6.26.0",
    "copy-to-clipboard": "^3.2.0",
    "hammerjs": "^2.0.8",
    "http-server": "^0.12.3",
    "jquery": "^3.5.1",
    "number-words": "^1.1.1",
    "resonance-audio": "^1.0.0",
    "s": "^1.0.0",
    "sono": "^2.1.6",
    "speak-tts": "^2.0.8",
    "unique-random": "^2.1.0"
  },
  "devDependencies": {
    "electron": "^9.4.0",
    "parcel-bundler": "^1.12.4"
  }
}
